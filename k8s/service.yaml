apiVersion: v1
kind: Service
metadata:
  name: teachlink-backend-service
  namespace: teachlink
  labels:
    app: teachlink-backend
spec:
  type: ClusterIP
  selector:
    app: teachlink-backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
