apiVersion: v1
kind: Service
metadata:
  name: teachlink-backend
  namespace: default
  labels:
    app: teachlink-backend
spec:
  selector:
    app: teachlink-backend # routes to both stable and canary pods
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
