apiVersion: apps/v1
kind: Deployment
metadata:
  name: teachlink-backend-green
  labels:
    app: teachlink-backend
    color: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: teachlink-backend
      color: green
  template:
    metadata:
      labels:
        app: teachlink-backend
        color: green
    spec:
      containers:
        - name: teachlink-backend
          image: your-dockerhub-username/teachlink-backend:latest
          imagePullPolicy: Always
          resources:
            limits:
              cpu: "250m"
              memory: "256Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
          ports:
            - containerPort: 3000
